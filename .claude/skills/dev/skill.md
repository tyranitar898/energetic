Start the local development server for the Energetic app.

## Steps

1. Run `npm run dev` in the background to start the Next.js dev server
2. Report the local URL (http://localhost:3000) to the user
